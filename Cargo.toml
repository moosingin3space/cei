[package]
name = "cei"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
clap = { version = "4.5.40", features = ["derive"] }
libc = "0.2.177"
nix = { version = "0.31.2", features = ["process", "ptrace", "signal", "socket", "uio"] }
